name: specs

on:
  push:
    branches: [main]
  pull_request:

jobs:
  spec-md:
    name: lint, test, and coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      # The adapter files (Cursor/Windsurf/Cline/Copilot/AGENTS.md and the
      # plugin skill copy) are generated from SKILL.md. Fail if they drifted.
      - name: Adapters in sync with SKILL.md
        run: node scripts/sync-adapters.mjs --check

      - name: Publish target consistency
        run: pnpm run publish:check

      # The zero-dependency CLI test suite.
      - name: Tests
        run: pnpm test

      # Dogfood: validate this repo's own specs, their [TC-N] coverage, and the
      # behavioral model — including conformance against the pizza example's
      # adapter, which imports src/*.ts directly (Node >= 22.18 strips types).
      - name: Validate specs
        run: node cli/bin/spec-md.js check . --strict --conform

  # Dedicated status check for branch protection. Version bump is applied on the
  # branch (`pnpm run changeset:version`), not after merge.
  changeset:
    name: Version bump required
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Require an applied version bump for shipped changes
        env:
          BASE_REF: ${{ github.base_ref }}
        run: node scripts/check-changeset-consistency.mjs --base "origin/$BASE_REF"
